import { render, screen } from '@testing-library/react';
import App from './App';

test.todo('it should render a loading screen while waiting for data', () => {
});
test.todo('it should keep a list of selected images', () => {
});
test.todo('it should submit the selected images for validation', () => {
});
test.todo('it should return to Screen 1 when invalid response received', () => {
});
test.todo('it should render a success message when a valid response is recieved', () => {
});
